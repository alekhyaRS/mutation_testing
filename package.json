
{
  "devDependencies": {
    "@stryker-mutator/core": "^8.2.6",
    "@stryker-mutator/html-reporter": "^3.1.0",
    "@stryker-mutator/jest-runner": "^8.2.6",
    "jest": "^29.7.0"
  },
  
  "scripts": {
    "test": "rmdir /S /Q .stryker-tmp && jest"
  },

    "scripts": {
      "test": "rimraf .stryker-tmp && jest"
    }
    }